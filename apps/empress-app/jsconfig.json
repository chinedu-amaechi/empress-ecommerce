{
  "compilerOptions": {
    "baseUrl": ".", // Ensure paths are resolved from the project root
    "paths": {
      // "@/*": ["./src/*"], // Keep existing alias for internal `src/`
      "@backend/*": ["../../backend/*"] // Allow access to backend from empress-app
    }
  }
}
